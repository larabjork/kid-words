<h1>Wacky, Wonderful Words</h1>

<h4>Word: <%= @word.term %></h4>



<h5>Definitions</h5>

  <% if @word.definitions %>
    <ul>
      <% @word.definitions.each do |definition| %>
        <li><a href="/words/<%= @word.id %>/definitions/<%= definition.id %>"><%= definition.definition %></a></li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no definitions listed for this word yet.</p>
  <% end %>

<h4>Add a definition:</h4>

<form action="/words/<%= @word.id %>/definitions" method="post">
  <div class="form-group">
    <label for="definition_term">Type your definition here.</label>
    <input id="definition_term" name="definition_term" class="form-control" type="text">
  </div>
  <button type="submit" class="btn">Add definition</button>
</form>


<p><a href="/words/<%= @word.id %>/edit">Edit word</a></p>
<a href="/words">Return to word list</a>
